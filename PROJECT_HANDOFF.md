# AA BATTLE 인수인계

## 목표

가상의 창작 포켓몬이 포텐셜을 사용해 싸우는 Windows 배틀 시뮬레이터를 만든다. 좌우 상단에 HeadKasen 폰트의 AA, 하단에 기술 선택, 중앙에 계산 버튼을 둔다. 시스템 문자는 Pokemon BW 폰트를 사용하고 흑백 고전 도트 디자인을 유지한다.

## 현재 상태

- 메인 실행 파일: `outputs/desktop/AABattle.exe`
- 빌드: `desktop/build.ps1`
- 팀 편집기는 메인 화면의 `팀 편집` 버튼으로 연다.
- 트레이너와 포켓몬을 `.aabdata.json`에 저장한다.
- 포켓몬 화면의 버튼으로 왼쪽 또는 오른쪽 배틀 슬롯에 적용한다.
- 트레이너 화면의 버튼으로 양쪽 트레이너를 적용한다. 적용 전에는 지령이 없으며, 적용된 트레이너의 활성화된 `지령` 슬롯만 배틀 선언 목록에 표시한다.
- 기술 행동에는 4식 1개와 지령 1개를 선택적으로 선언할 수 있다. 각각 원칙적으로 `1/시`(1시합 1회)이며 사용하지 않고 행동을 확정해도 된다. 사용 이력은 시합 동안 유지되고, 다른 포텐셜에 의한 보충은 향후 포텐셜 실행기에서 처리한다.
- 『돌아와!』는 교대 대상과 함께 선언하고 상대의 행동 후 교대한다. 통상 교대는 행동 전에 처리한다.
- 기본 4식·숙달/전개 6령과 선택 규칙인 『맞춰라!』·저격/회피 지령을 실행한다.
- 팀 편집기의 능력치 입력은 종족값이다. 슬롯 변환 시 IV 31, EV 0, 성격 무보정이다.
- 타입이나 기술이 참조 데이터에 없으면 제외하며, 유효 항목이 하나도 없으면 적용을 중단한다.
- 포텐셜은 이름, 슬롯, 발동 방식, 사용 제한, 트리거, 효과, 원문으로 분리해 저장한다.
- 지령 이외의 포텐셜은 배틀 데이터에 전달되지만 아직 실행되지 않는다.
- 포켓몬 포텐셜 슬롯은 `역할 → 분류 → 주인 → 이명 → 종족①~④ → 속별 → 유대 → 선 → 회피 → 내성 → 격 → 범용 → 부수 → 특권 → PT①~②` 순서다. 고유·전용·지령은 트레이너 포텐셜이다.
- 데이터 편집기의 `포텐셜 종류`는 선택식이다. 포켓몬에는 위 19종, 트레이너에는 고유·전용·지령만 표시하며 기존 파일의 예전 분류값은 보존한다.
- 포켓몬의 `역할` 행은 자유 작명하지 않는다. 오레마스 역할 35개와 아바돈 역할 7개 중 하나를 선택하면 표준 효과가 자동 입력된다.
- 아바돈의 `헌트` 6종은 역할이 아니라 `부수`다. 탐사대원·탐사대장은 프라이멀 헌트를 공유하고, 수렵/보급/구급/추적/조사대원은 각각 와일드/브레이크/레스큐/새비지/리서치 헌트와 연결된다. 역할 선택 시 해당 부수 행을 자동 입력한다. 추가 역할 5종은 통솔 A 이상을 요구한다.
- `범용`은 기합·초근성·전투속행·진지작성·군략·초기합 6종, `선`은 선의 선·대의 선·후의 선·삼종의 신기 4종 중에서 선택하며 효과를 자동 입력한다. 삼종의 신기는 육성 AA 이상 전용이다.
- 포텐셜 편집 표에서는 대상 타입·발동 방식·횟수 열을 제거했다. `회피`·`내성`·`격`의 타입은 포텐셜 이름과 트리거 조건에 직접 표현한다. 구버전의 `targetType`, `activation`, `uses` 데이터는 파일 호환을 위해 읽되 새 저장에서는 대상 타입을 비운다.
- `발동 금지` 도트 체크를 켜면 `enabled=false`로 저장하며 배틀 슬롯 변환에서 제외한다. 체크를 끄면 다시 실행 후보가 된다.
- 팀 편집기의 `기술·효과 도감`은 등록된 전체 기술을 검색·상세 확인·4개까지 삽입할 수 있고, 내장 및 현재 프로젝트의 포텐셜 트리거·효과를 검색해 선택한 포텐셜 행에 입력할 수 있다.
- 방호 포텐셜은 기술 무효/반사, 버텨라 계열, 방어·특방 강화, 위력·대미지 완화 효과다. 회피, 대미지 유예, 피격 반격, 『기합』은 제외한다. 포켓몬 포텐셜에서 나온 방호 관통은 트레이너 포텐셜의 방호 효과와 『버텨라!』를 관통하지 못한다.

## 계산 규칙

기본 능력치와 대미지는 첨부된 슈퍼 마개조 계산기 Ver 3.617을 기준으로 한다. 낮은 레벨과 레벨 차 1% 보정, 시트의 내림 순서, 85~100 난수, 급소 2배를 유지한다. 일반 상태와 날씨는 현대 싱글 규칙을 참고하되 시트 확장인 동상과 과거 싸라기눈을 별도 지원한다.

현재 회귀 검사에는 원본 시트의 290~342 대미지 및 난수 0.87에서 297, 타입 무효, 날씨·필드, 상태, 대타, 행동 순서, 팀 편집기 종족값 변환이 포함된다.

## 포텐셜 연구

최신 도감 UI는 **트리거 조건 / 효과** 두 독립 목록으로 단순화했다. `PotentialParts.cs`가 원문 기반 구성요소를 정규화하고 합친다. 전각·공백·말미 구두점·단일 절 번호 표기를 통합하지만 확률·횟수·배율 등은 보존한다. 기본 구성요소는 조건 1,296개 / 효과 3,179개이며 `potential-parts.json`에 내보냈다. 현재 편집 데이터는 런타임에 같은 규칙으로 합쳐진다. 조건을 입력할 때 내부 발동 방식과 횟수도 함께 적용하고, 효과 입력은 조건을 덮어쓰지 않는다. 새 조합의 원문은 수집 원문으로 오인되지 않도록 비운다.

도감의 표·상세·검색 표시에는 Pokemon BW 글꼴을 사용한다. 해당 OTF는 기본 GDI 텍스트 렌더러에서 글자가 깨지므로 GDI+로 그린다. 검색 입력 중에는 한글 IME를 지원하는 기본 입력기를 사용하고, 포커스를 벗어나면 도트 글꼴로 표시한다. 이전의 이름·종류·출처 열은 도감에서 제거했으며 원본 자료에는 보존한다.

Codex 작업 지침과 검증 절차는 `AGENTS.md`, `HARNESS.md`, `desktop/verify.ps1`을 참고한다. 구성요소 재생성: `AABattleDataEditor.exe --export-components research/potentials/potential-parts.json`.

팀 편집기의 도감은 `기술 도감`과 `포텐셜 도감`으로 분리했다. `desktop/CatalogUI.cs`가 검색, 상세 표시와 흑백 도트 스크롤을 담당한다. 기술은 표에서 타입·분류·위력·명중·우선도·효과를 비교한다. 포텐셜 연구 JSON은 두 EXE에 `potentials.json` 리소스로 내장되어 다른 PC에서도 4,151개 정의를 기반으로 구성요소를 생성한다. 사용자 제공 지령 양식도 구성요소에 포함된다.

고정 포텐셜 선택과 기존 데이터 로딩 시, 효과에 포함된 명시적 조건은 트리거로 분리하고 원문을 보존한다. 문장 속 `임의의`를 발동 방식으로 제거하지 않는다. 도감의 트리거/효과 부분 입력은 기존 원문을 덮어쓰지 않는다. 자동 분리 결과는 실행 규칙 확정을 의미하지 않는다.

2026-09-09 감사: 모든 4,151개 정의의 이름과 정규화된 원문이 보관 출처에 존재했다. 출처가 있는 482개 페이지의 캐시 누락은 없었다. 두 목록 페이지와 역할/유키나리 표본 페이지는 현재 온라인 원문과 동일했다. 1,773개 정의는 기존 검토 필요 표시를 유지한다. 상세 결과는 `research/potentials/catalog-audit.json`, 재검증 스크립트는 `audit_catalog.py`이다. 이 검사는 수집 자료의 출처 일치를 확인하며 모든 자연어 효과의 엔진 해석까지 검증하지는 않는다.

`research/potentials/potential-occurrences.json`에는 아바돈 캐릭터 시트에서 수집한 포텐셜 출현이 있다. `potential-catalog.json`은 이름과 원문 효과를 기준으로 중복을 합친 목록이다. `MODEL.md`는 실행 이벤트 구조 초안이다.

자동으로 나눈 트리거와 효과는 색인 초안이다. `raw`가 최종 기준이며 `activation.confidence=review` 또는 `needs_review`가 있는 정의는 사람이 확인한 뒤 엔진 규칙으로 변환한다.

## 다음 구현 순서

1. `.aabdata.json`에서 완성된 6마리 팀을 배틀 세션 단위로 한 번에 불러온다.
2. 교대 시 해제·유지되는 상태변화를 세부 규칙에 맞게 확장한다.
3. 포텐셜 이벤트 큐를 `배틀 시작 → 등장 → 기술 결정 → 행동 전 → 명중/피격 → 대미지 후 → 빈사 → 교대 → 턴 종료` 순서로 만든다.
4. 방호 포텐셜의 출처와 방호 관통 범위를 효과 단위로 구현한다.
5. 사용자가 제공하는 나머지 표준 포텐셜을 선택 목록과 효과 도감에 추가한다.

## 다른 컴퓨터에서 시작

비공개 Git 저장소는 `https://github.com/unistcetacea-dev/aa-battle`이다. 다른 컴퓨터에서 같은 GitHub 계정으로 로그인한 뒤 다음 명령으로 복제한다.

```powershell
git clone https://github.com/unistcetacea-dev/aa-battle.git
cd aa-battle
./desktop/build.ps1
```

저장소 루트에서 Codex 작업을 열고, 새 작업의 첫 메시지에서 `README.md`와 `PROJECT_HANDOFF.md`를 먼저 읽도록 요청한다. 빌드 후 두 self-test를 통과시킨 뒤 작업을 계속한다.

작업을 시작하기 전에는 `git pull`, 마친 뒤에는 커밋과 `git push`를 수행해 두 컴퓨터의 소스를 맞춘다. 같은 파일을 두 컴퓨터에서 동시에 편집하지 않는다.

Codex 공유 링크는 특정 시점의 읽기 전용 대화 스냅샷이다. 향후 결정은 이 문서에도 갱신하여 대화 링크 없이도 개발을 이어갈 수 있게 한다.
