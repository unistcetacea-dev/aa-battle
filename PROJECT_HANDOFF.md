# AA BATTLE 인수인계

## 목표

가상의 창작 포켓몬이 포텐셜을 사용해 싸우는 Windows 배틀 시뮬레이터를 만든다. 좌우 상단에 HeadKasen 폰트의 AA, 하단에 기술 선택, 중앙에 계산 버튼을 둔다. 시스템 문자는 Pokemon BW 폰트를 사용하고 흑백 고전 도트 디자인을 유지한다.

## 현재 상태

- 메인 실행 파일: `outputs/desktop/AABattle.exe`
- 빌드: `desktop/build.ps1`
- 팀 편집기는 메인 화면의 `팀 편집` 버튼으로 연다.
- 트레이너와 포켓몬을 `.aabdata.json`에 저장한다.
- 포켓몬 화면의 버튼으로 왼쪽 또는 오른쪽 배틀 슬롯에 적용한다.
- 트레이너 화면의 버튼으로 양쪽 트레이너를 적용한다. 적용 전에는 지령이 없으며, 적용된 트레이너의 활성화된 `지령` 슬롯만 배틀 선언 목록에 표시한다.
- 기술 행동에는 4식 1개와 지령 1개를 선택적으로 선언할 수 있다. 각각 원칙적으로 `1/시`(1시합 1회)이며 사용하지 않고 행동을 확정해도 된다. 사용 이력은 시합 동안 유지되고, 다른 포텐셜에 의한 보충은 향후 포텐셜 실행기에서 처리한다.
- 『돌아와!』는 교대 대상과 함께 선언하고 상대의 행동 후 교대한다. 통상 교대는 행동 전에 처리한다.
- 기본 4식·숙달/전개 6령과 선택 규칙인 『맞춰라!』·저격/회피 지령을 실행한다.
- 팀 편집기의 능력치 입력은 종족값이다. 슬롯 변환 시 IV 31, EV 0, 성격 무보정이다.
- 타입이나 기술이 참조 데이터에 없으면 제외하며, 유효 항목이 하나도 없으면 적용을 중단한다.
- 포텐셜은 이름, 슬롯, 발동 방식, 사용 제한, 트리거, 효과, 원문으로 분리해 저장한다.
- 지령 이외의 포텐셜은 배틀 데이터에 전달되지만 아직 실행되지 않는다.
- 포켓몬 포텐셜 슬롯은 `역할 → 분류 → 주인 → 이명 → 종족①~④ → 속별 → 유대 → 선 → 회피 → 내성 → 격 → 범용 → 부수 → 특권 → PT①~②` 순서다. 고유·전용·지령은 트레이너 포텐셜이다.
- 데이터 편집기의 `포텐셜 종류`는 선택식이다. 포켓몬에는 위 19종, 트레이너에는 고유·전용·지령만 표시하며 기존 파일의 예전 분류값은 보존한다.
- 포켓몬의 `역할` 행은 자유 작명하지 않는다. 오레마스 역할 35개와 아바돈 역할 7개 중 하나를 선택하면 표준 효과가 자동 입력된다.
- 아바돈의 `헌트` 6종은 역할이 아니라 `부수`다. 탐사대원·탐사대장은 프라이멀 헌트를 공유하고, 수렵/보급/구급/추적/조사대원은 각각 와일드/브레이크/레스큐/새비지/리서치 헌트와 연결된다. 역할 선택 시 해당 부수 행을 자동 입력한다. 추가 역할 5종은 통솔 A 이상을 요구한다.
- `범용`은 기합·초근성·전투속행·진지작성·군략·초기합 6종, `선`은 선의 선·대의 선·후의 선·삼종의 신기 4종 중에서 선택하며 효과를 자동 입력한다. 삼종의 신기는 육성 AA 이상 전용이다.
- 포텐셜 편집 표의 `상대 타입` 열은 `회피`·`내성`·`격`에서 활성화된다. 포텐셜 종류 → 상대 타입 → 표준 이름 순서로 선택하며 `targetType`을 저장한다. 발동 방식과 횟수는 내부 데이터로 보존한다.
- `발동 금지` 도트 체크를 켜면 `enabled=false`로 저장하며 배틀 슬롯 변환에서 제외한다. 체크를 끄면 다시 실행 후보가 된다.
- 팀 편집기의 `기술·효과 도감`은 등록된 전체 기술을 검색·상세 확인·4개까지 삽입할 수 있고, 내장 및 현재 프로젝트의 포텐셜 트리거·효과를 검색해 선택한 포텐셜 행에 입력할 수 있다.
- 방호 포텐셜은 기술 무효/반사, 버텨라 계열, 방어·특방 강화, 위력·대미지 완화 효과다. 회피, 대미지 유예, 피격 반격, 『기합』은 제외한다. 포켓몬 포텐셜에서 나온 방호 관통은 트레이너 포텐셜의 방호 효과와 『버텨라!』를 관통하지 못한다.

## 계산 규칙

기본 능력치와 대미지는 첨부된 슈퍼 마개조 계산기 Ver 3.617을 기준으로 한다. 낮은 레벨과 레벨 차 1% 보정, 시트의 내림 순서, 85~100 난수, 급소 2배를 유지한다. 일반 상태와 날씨는 현대 싱글 규칙을 참고하되 시트 확장인 동상과 과거 싸라기눈을 별도 지원한다.

현재 회귀 검사에는 원본 시트의 290~342 대미지 및 난수 0.87에서 297, 타입 무효, 날씨·필드, 상태, 대타, 행동 순서, 팀 편집기 종족값 변환이 포함된다.

## 포텐셜 연구

최신 도감 UI는 왼쪽의 **트리거 조건 목록**과 오른쪽의 **구조화 효과 편집기**로 구성한다. 효과 원문 검색·상세·원문 입력 버튼은 제거했다. `PotentialParts.cs`는 연구 및 구현 도감용 원문 구성요소를 계속 정규화하고 보존한다. 전각·공백·말미 구두점·단일 절 번호 표기를 통합하지만 확률·횟수·배율 등은 보존한다. 기본 구성요소는 조건 1,296개 / 효과 3,179개이며 `potential-parts.json`에 내보냈다. 조건을 입력할 때 내부 발동 방식과 횟수도 함께 적용하고, 구조 효과 입력은 조건을 덮어쓰지 않는다. 새 조합의 원문은 수집 원문으로 오인되지 않도록 비운다.

2026-09-15 현재 정규화 결과는 조건 1,270개 / 효과 3,159개다. 이 중 53개를 구현 도감에서 제외해 구현 대상은 3,106개다. 연구 원문 4,151개와 출처는 보존한다. 역할·범용·선·회피/내성/격·부수·지령 템플릿에서 나온 효과 129개는 `protectedTemplate=true`로 자동 보호하며 도감에 `[기본 보호]`로 표시한다. 보호 효과는 중복 정리나 구현 분류 과정에서 삭제·치환하지 않는다.

효과 정리는 연구 원문을 삭제하지 않고 `excludedFromImplementation`과 제외 사유를 기록한다. 독립 주석, 편성/공유 규칙, 배틀 외 경험치/상금 획득 규칙만 구현 효과 도감에서 숨기며 `protectedTemplate` 항목이 항상 우선한다.

상단 `효과 구현 방식` 버튼과 같은 이름의 탭은 `EffectImplementationDex`를 연다. 제외 후 3,106개 효과를 자동 구현/일부 자동 구현/구현 규칙 준비/기준 질문 필요/미구현으로 표시하고, 선택한 효과의 해석·현재 코드 처리·출처를 보여 준다. 현재 포켓몬의 자연어 포텐셜 효과는 자동 실행되지 않으며, 트레이너 4식·지령만 `Engine.Resolve`/`Engine.ApplyOrder`에서 실행된다.

`PotentialProbability`는 드물게=10%, 저확률=20%, 중확률=50%로 정의한다. 한 포켓몬에서 같은 발동 시점에 성립한 중확률 후보가 2개 이상이고 독립 판정이 전부 실패하면 그중 하나를 무작위로 강제 발동한다. 원래 판정에서 여러 개가 성공했다면 모두 발동한다. 고확률은 지원하지 않으며 해당 표현은 원문을 보존한 채 구현 도감에서 제외한다. 현재 구현 대상에는 고확률 표현이 없다.

`EffectStructures`는 기본 보호 효과를 건드리지 않고 단순 배율 문장, 단순 최대 HP 분수 문장, 단순 능력 랭크 변화 문장을 구조화한다. 포텐셜 도감에서는 효과 원문을 찾지 않고 구조, 대상, 동작, 배율/N/랭크를 직접 정한다. 현재 구조는 능력 랭크 변화, 종족치 변경, 수치 배율, 최대 HP의 1/N이며 구조를 고르면 가능한 대상과 동작만 표시한다. `FractionalHpRule`은 최대 HP/N의 소수점을 버리고 최소 1로 처리한다. 대미지는 기본적으로 빈사가 가능하고, 빈사 불가로 지정한 효과만 HP 1에서 멈춘다. 랭크 변화는 오른다/내린다=±1, 크게 오른다/내린다=±2, 매우 크게 오른다/내린다=±3으로 정규화한다. 쭉 오른다/급격히 오른다는 원문만 보존하고 구현 목록에서 제외한다. 급소 확률, 트레이너 지시처럼 포켓몬 전투 능력치가 아닌 대상은 이 구조에 합치지 않는다. 복합 효과와 기본 보호 효과의 원문은 유지한다.

능력 선택 표현은 두 구조로 구분한다. `임의의 능력`은 사용자가 공격·방어·특공·특방·속도·명중·회피·C 중 하나를 선택한다. `랜덤한 능력치`는 공격·방어·특공·특방·속도 중 하나를 같은 확률로 자동 선택한다. `Fighter.CriticalStage`가 C 랭크를 저장하고 전장 전체 현황에 표시한다. 자연어 포텐셜 실행기와 급소 계산 연결은 아직 준비 단계다.

`가장 높은/낮은 능력`은 공격·방어·특공·특방·속도의 `Engine.Effective` 값을 비교한다. 현재 랭크, 직접 강화·약화 배율, 상태와 장소 보정이 반영되며 동률 후보는 같은 확률로 자동 선택한다. `가장 높은/낮은 종족치` 문장은 이 규칙에 합치지 않는다.

`가장 높은/낮은 종족치`는 포켓몬의 원래 공격·방어·특공·특방·속도 종족값을 비교하고, 동률 후보 중 하나를 같은 확률로 자동 선택한다. 랭크와 배율은 반영하지 않는다.

`SpeciesStatRanks`는 종족치 1·2랭크 상승 범위를 계산한다. 기존의 `-`, 무표기, `+`를 유지해 지정한 수만큼 문자 등급을 이동한다. 예를 들어 B-=90은 1랭크에서 A-=110, 2랭크에서 AA-=140이 된다. B+=105~109는 1랭크에서 A+=135~139, 2랭크에서 AA+=165~169가 된다. S는 200~999로 제한한다. 구조화 편집기에서 현재 포켓몬과 대상 종족치를 읽어 이 범위 안의 값만 입력받는다.

도감의 표·상세·검색 표시에는 Pokemon BW 글꼴을 사용한다. 해당 OTF는 기본 GDI 텍스트 렌더러에서 글자가 깨지므로 GDI+로 그린다. 검색 입력 중에는 한글 IME를 지원하는 기본 입력기를 사용하고, 포커스를 벗어나면 도트 글꼴로 표시한다. 이전의 이름·종류·출처 열은 도감에서 제거했으며 원본 자료에는 보존한다.

Codex 작업 지침과 검증 절차는 `AGENTS.md`, `HARNESS.md`, `desktop/verify.ps1`을 참고한다. 구성요소 재생성: `AABattleDataEditor.exe --export-components research/potentials/potential-parts.json`.

팀 편집기의 도감은 `기술 도감`과 `포텐셜 도감`으로 분리했다. `desktop/CatalogUI.cs`가 검색, 상세 표시와 흑백 도트 스크롤을 담당한다. 기술은 표에서 타입·분류·위력·명중·우선도·효과를 비교한다. 포텐셜 연구 JSON은 두 EXE에 `potentials.json` 리소스로 내장되어 다른 PC에서도 4,151개 정의를 기반으로 구성요소를 생성한다. 사용자 제공 지령 양식도 구성요소에 포함된다.

고정 포텐셜 선택과 기존 데이터 로딩 시, 효과에 포함된 명시적 조건은 트리거로 분리하고 원문을 보존한다. 문장 속 `임의의`를 발동 방식으로 제거하지 않는다. 도감의 트리거/효과 부분 입력은 기존 원문을 덮어쓰지 않는다. 자동 분리 결과는 실행 규칙 확정을 의미하지 않는다.

2026-09-09 감사: 모든 4,151개 정의의 이름과 정규화된 원문이 보관 출처에 존재했다. 출처가 있는 482개 페이지의 캐시 누락은 없었다. 두 목록 페이지와 역할/유키나리 표본 페이지는 현재 온라인 원문과 동일했다. 1,773개 정의는 기존 검토 필요 표시를 유지한다. 상세 결과는 `research/potentials/catalog-audit.json`, 재검증 스크립트는 `audit_catalog.py`이다. 이 검사는 수집 자료의 출처 일치를 확인하며 모든 자연어 효과의 엔진 해석까지 검증하지는 않는다.

`research/potentials/potential-occurrences.json`에는 아바돈 캐릭터 시트에서 수집한 포텐셜 출현이 있다. `potential-catalog.json`은 이름과 원문 효과를 기준으로 중복을 합친 목록이다. `MODEL.md`는 실행 이벤트 구조 초안이다.

자동으로 나눈 트리거와 효과는 색인 초안이다. `raw`가 최종 기준이며 `activation.confidence=review` 또는 `needs_review`가 있는 정의는 사람이 확인한 뒤 엔진 규칙으로 변환한다.

## 다음 구현 순서

1. 팀 보관함에 복사된 포켓몬이 원본 포켓몬 수정 내용을 안전하게 따라가도록 안정적인 데이터 ID 참조 방식을 설계한다.
2. 교대 시 해제·유지되는 상태변화를 세부 규칙에 맞게 확장한다.
3. 포텐셜 이벤트 큐를 `배틀 시작 → 등장 → 기술 결정 → 행동 전 → 명중/피격 → 대미지 후 → 빈사 → 교대 → 턴 종료` 순서로 만든다.
4. 방호 포텐셜의 출처와 방호 관통 범위를 효과 단위로 구현한다.
5. 사용자가 제공하는 나머지 표준 포텐셜을 선택 목록과 효과 도감에 추가한다.

## 다른 컴퓨터에서 시작

비공개 Git 저장소는 `https://github.com/unistcetacea-dev/aa-battle`이다. 다른 컴퓨터에서 같은 GitHub 계정으로 로그인한 뒤 다음 명령으로 복제한다.

```powershell
git clone https://github.com/unistcetacea-dev/aa-battle.git
cd aa-battle
./desktop/build.ps1
```

저장소 루트에서 Codex 작업을 열고, 새 작업의 첫 메시지에서 `README.md`와 `PROJECT_HANDOFF.md`를 먼저 읽도록 요청한다. 빌드 후 두 self-test를 통과시킨 뒤 작업을 계속한다.

작업을 시작하기 전에는 `git pull`, 마친 뒤에는 커밋과 `git push`를 수행해 두 컴퓨터의 소스를 맞춘다. 같은 파일을 두 컴퓨터에서 동시에 편집하지 않는다.

Codex 공유 링크는 특정 시점의 읽기 전용 대화 스냅샷이다. 향후 결정은 이 문서에도 갱신하여 대화 링크 없이도 개발을 이어갈 수 있게 한다.

## 2026-09-15 시작 흐름 / 등장 트리거
- 기본 실행은 포켓몬 AA 배틀 시작 화면 → 팀 편집 또는 배틀 시작이다.
- 팀 편집의 `팀 데이터` 탭은 이름별 팀 보관함이다. 팀마다 트레이너·AA·포켓몬 1체 이상을 저장하며 상한은 없다. 시작 화면의 별도 1팀·2팀 선택 칸이 보관함의 팀 데이터를 불러온다. 편성은 저장 시점의 팀 데이터이므로 수정한 포켓몬은 다시 편성한다.
- 배틀 시작은 좌우 트레이너 AA 진입·충돌 흔들림 → 실제 참가 수만큼 몬스터볼 순차 표시 → 양쪽 선발 지정 → 팀 전체 로드 순서다.
- 쓰러진 진영의 지령/교대 버튼에서 죽어내밀기를 지정한다. 교체 자체는 턴을 진행하지 않는다.
- EntryTriggers는 field.enter / 장소에 나왔을 때를 관리한다. 선발·교대·죽어내밀기 경로에서 공통 호출한다. 조건 충족을 로그로 기록하며 자연어 효과는 판정 대기다. 강제 교대는 향후 같은 API를 사용한다.
- PotentialParts는 필드/장소·등장 어미 별칭과 공백을 정규화하되 추가 조건·확률·횟수를 보존한다. 원문/출처는 그대로 남긴다. 관리 규칙: research/potentials/TRIGGERS.md.
- 상태·날씨·효과 도감은 시작 화면 위쪽과 편집기 상단에서 바로 연다. 상태이상/상태변화/진영 상태/날씨/필드/전체 장소/연계/도구·특성 상태에 더해 강화·약화·중첩·랭크와 배율의 차이·방호 관통을 설명한다.
- `EffectNormalizer`는 배율 표현과 단순 랭크 상승 표현을 표준 문장으로 합친다. 종족치를 특정 랭크까지 바꾸는 효과와 복합 문장은 별도 구조가 정해질 때까지 합치지 않는다. 구조 효과 입력기는 포텐셜 도감 안에 있다.
- verify.ps1 -Visual은 기존 회귀 검사와 8체·9체 팀 저장 복원, 좌우 선발, 대기 7체·8체 검사 및 시작/편성/VS/선발/상태 도감/효과 구조 선택 캡처를 함께 수행한다.
- 배틀창 오른쪽의 `전장 전체 현황`은 Pokemon BW 글꼴로 전체 장소, 양쪽 상태이상·상태변화, 랭크, 직접 강화·약화 배율, 선택 기술의 위력·능력·자속·상성·방호·최종 대미지 배율, 턴 종료 대미지와 회복을 한 화면에 표시한다. 등록 포텐셜은 자연어 실행기 연결 전까지 `판정 대기`, 실제 실행 상태는 별도 줄로 표시한다. 스텔스록·압정(최대 3겹)·독압정(최대 2겹)·끈적끈적네트는 일반 포켓몬 규칙으로 설치·교대 발동하며, 통굽부츠·매직가드·접지·독/강철 타입 예외와 고속스핀·안개제거 제거를 처리한다.
